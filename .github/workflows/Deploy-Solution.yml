name: deploy-solution

on: [push]

jobs:
  my_job:
    runs-on: windows-latest
    steps:
      - name: Set environment variable
        run: echo "MY_VARIABLE=some_value" >> $env:GITHUB_ENV
      - name: Check condition
        if: env.MY_VARIABLE == 'some_value'
        run: echo "Condition is true!"
